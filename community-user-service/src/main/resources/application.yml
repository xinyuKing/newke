server:
  port: 8081
  servlet:
    context-path: /community

spring:
  application:
    name: user-service
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/study?characterEncoding=utf-8&useSSL=false&serverTimezone=Hongkong&allowPublicKeyRetrieval=true
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
      idle-timeout: 30000
  redis:
    database: 11
    host: localhost
    port: 6379
  mail:
    host: smtp.163.com
    port: 465
    username: jindashenzhishen@163.com
    password: PFTOKOIQJHQEAWVM
    protocol: smtps
    properties:
      mail:
        smtp:
          ssl:
            enables: true
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.nowcoder.community.entity
  configuration:
    use-generated-keys: true
    map-underscore-to-camel-case: true

community:
  path:
    domain: http://localhost:8080
    upload: d:/work/data/upload

management:
  endpoints:
    web:
      exposure:
        include: '*'
        exclude: info,caches
