server:
  port: 8085

spring:
  application:
    name: data-service
  redis:
    host: localhost
    port: 6379
    database: 11
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'
        exclude: info,caches
