# Dev Scripts

Current helper script:

- `print-startup-order.ps1`: prints the recommended module startup order after the repository restructure.
- `verify-structure.py`: validates the new `apps/services/shared/platform` layout and checks Maven `relativePath` / `module` references.
