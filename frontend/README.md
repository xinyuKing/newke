# Community Vue 3 Frontend

## Setup

```bash
npm install
npm run dev
```

Default dev server runs at `http://localhost:5173` and proxies `/api`, `/kaptcha`, `/share` to the Spring Boot backend at `http://localhost:8080/community`.

## Build

```bash
npm run build
npm run preview
```
